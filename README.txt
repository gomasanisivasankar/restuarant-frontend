1.At first download the frontend and backend zip files from the git hub and unzip them

2.For both frontend and backend to install the dependency node modules the command: npm intall

3.To run the frontend the command will be: npm run start

4.To run the backend the command will be : nodemon index.js or node index.js